[project]
name = "multi-agent-debates"
version = "0.1.0"
description = "Multi-agent debate system for document refinement."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "langchain-anthropic",
  "langchain-core",
  "langgraph",
  "langsmith",
  "python-dotenv",
]
